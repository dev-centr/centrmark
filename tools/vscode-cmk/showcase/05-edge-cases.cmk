// Edge cases for editor highlighting behavior

// Close marker without open (should not crash depth provider)
:::

::: section id="attrs-and-inline"
Inline-looking content with symbols: <div>not HTML parsing</div> and ::: inside text.
Escaped-ish sequence: \::: remains plain text.
:::

::: section id="unterminated-demo"
This block is intentionally left open in this sample file
to verify folding/highlighting behavior on partial documents.
